[medium, doc-vs-impl + perf] gix-store/src/kind.rs:467-503 — list_under's doc claims it scans 'only that subtree's refs instead of every entity of the kind', but list_entries_in (kind.rs:492) calls source_entries(), which enumerates *every* entity ref of the kind, and then filters by prefix. Same for entries_under. The narrowed scan the doc promises would be refs().prefixed(&self.group_prefix(group)) directly. As written, a kind with 100k entities pays the full scan to list a 3-entity group. Either restore the prefixed scan or fix the doc.
