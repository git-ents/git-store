[low, ux] git-store/src/main.rs:224-236 + 459-464 — 'git store schema put <kind>' with no -F/-i at a terminal silently blocks: read_source falls through to read_stdin with no prompt, so the command appears hung. The sibling 'put' path handles exactly this case by detecting the TTY and opening $EDITOR (gathered_json, main.rs:304-317). Either give schema put the same TTY fallback, or at minimum print 'reading schema JSON from stdin…' to stderr.
