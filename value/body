[low, doc] gix-store/src/kind.rs:444-465 (fingerprint) — the doc says 'without reading any Git objects', but fingerprint() calls list_entries(), which (a) always performs the full entity-ref scan and (b) reads and decodes the index *tree object* whenever the index ref exists (read_validated -> read_tree -> tree_entries). So it reads Git objects and is not cheaper than the listing it digests. Either compute the fingerprint from refs alone (skip read_validated — source_entries is sufficient and identical by the equality invariant) or fix the claim.
